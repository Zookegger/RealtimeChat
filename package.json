{
	"name": "realtimechat",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "concurrently \"npm run server\" \"npm run client\"",
		"server": "cd server && node src/index.js",
		"client": "cd client && npm run start",
		"dev": "concurrently \"cd server && nodemon src/index.js \" \"npm run client\"",
		"install-all": "npm install && cd server && npm install && cd ../client && npm install"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Zookegger/RealtimeChat.git"
	},
	"author": "Nguyen Duc Trung",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/Zookegger/RealtimeChat/issues"
	},
	"homepage": "https://github.com/Zookegger/RealtimeChat#readme",
	"description": "",
	"devDependencies": {
		"concurrently": "^9.2.0"
	},
	"dependencies": {
		"dotenv": "^17.2.1"
	}
}
