import { Container, Typography } from "@mui/material";

export const UserHomeView = () => {
    return (
        <Container>
            <Typography>User Page</Typography>
            <Typography>Welcome to AppName</Typography>
        </Container>
    );
}